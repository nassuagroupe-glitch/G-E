// Reserved for a future contextBridge API (e.g. native barcode-scanner
// access, thermal-printer spooling for the 80 mm ticket). Nothing is
// exposed to the renderer yet — the web app runs unmodified.
